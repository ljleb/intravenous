// Intentionally empty. Graph authoring is header-only so it can execute during
// constant evaluation; this translation unit only keeps the module support
// shared-library target materialized for the runtime module toolchain.
