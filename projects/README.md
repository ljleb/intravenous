# Projects

Checked-in Intravenous example projects live here.

Each project is intended to be opened as a VS Code workspace directly.

Current projects:

- `simple_sine`
  - minimal audible smoke-test project
  - loads paused
  - use the VS Code command palette command `Intravenous: Resume Playback`
